import {Button,Dialog,DialogActions,DialogContent,DialogTitle,Table,TableBody,TableCell,TableContainer,TableHead,TableRow} from '@mui/material';
import {formatPrice} from '../../utils/catalog.js';
export default function ReturnDetails({document:d,onClose}){
  return <Dialog open fullWidth maxWidth="md" onClose={onClose}><DialogTitle>Return {d.reference}</DialogTitle><DialogContent dividers><p>{d.invoice_number} · {d.contact_name||'Walk-in'} · {d.returned_at}</p><p className="my-3">Read-only return document</p><TableContainer><Table size="small" aria-label="Return details"><TableHead><TableRow>{['Product','Quantity','Historical unit price / cost','Gross value','Return adjustment'].map((s)=><TableCell key={s}>{s}</TableCell>)}</TableRow></TableHead><TableBody>{d.items.map((i)=><TableRow key={i.id}><TableCell>{i.sku} · {i.model}</TableCell><TableCell>{i.quantity}</TableCell><TableCell>{formatPrice(i.unit_price)}</TableCell><TableCell>{formatPrice(i.gross_value)}</TableCell><TableCell>{formatPrice(i.return_value)}</TableCell></TableRow>)}</TableBody></Table></TableContainer><p className="my-4 font-semibold">Total: {formatPrice(d.total)}</p><p className="whitespace-pre-wrap break-words">{d.notes||'No notes'}</p></DialogContent><DialogActions><Button onClick={onClose}>Close</Button></DialogActions></Dialog>;
}
